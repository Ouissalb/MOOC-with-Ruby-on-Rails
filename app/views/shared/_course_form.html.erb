<%= form_for(@course) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Enter the course's video link..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %> 
